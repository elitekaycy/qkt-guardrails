# One entry per account, named after the var each configs/*.yaml references.
# Never commit the real .env.
GUARDIAN_BOT2_FORWARD_BENCH_API_KEY=

# Optional, shared across every account that enables notify: in its config.
TG_TOKEN=
TG_CHAT=
